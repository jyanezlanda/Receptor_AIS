Contains the embedded firmware running on the platform processors.

Typical contents:

application code

board support packages

drivers

configuration files

unit tests

build scripts

Example structure:

platform-rx-fw
├── src/
├── drivers/
├── bsp/
├── configs/
├── tests/
└── tools/
